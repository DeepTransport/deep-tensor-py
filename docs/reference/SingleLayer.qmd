# SingleLayer { #deep_tensor.SingleLayer }

```python
SingleLayer(self)
```

Constructs the DIRT using a single layer.

In this setting, the DIRT algorithm reduces to the SIRT algorithm; 
see @Cui2022.